<% if Favorite.find_by(food_id: food.id, user_id: current_user.id).present? %>
  <td><%= link_to favorite_path(id: food.id), method: :delete, remote: true do %>★<% end %></td>
<% else %>
  <td><%= link_to favorites_path(id: food.id), method: :post, remote: true do %>☆<% end %></td>
<% end %>