<% if Relationship.find_by(followed_id: food.user.id, follow_id: current_user.id).present? %>
  <%= link_to "解除", relationship_path(id: food.id), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>
<% elsif !( food.user_id == current_user.id) %>
  <%= link_to "フォロー", relationships_path(id: food.id), method: :post, remote: true, class: "btn btn-sm btn-success" %>
<% end %>